# MongoDB — share saved channels + download history across machines.
# Use the SAME cluster as automation-server. Leave unset to use local JSON files.
MONGODB_URI=mongodb+srv://USER:PASS@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=youtube_automation
